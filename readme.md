# 🎓 Machine Learning Capstone Project App
An interactive multi-page data application featuring serialized inference weights and dynamic analytics dashboards.

## 🚀 Technical Setup Commands
1. Clone this public repository.
2. Install the necessary runtime engines: `pip install -r requirements.txt`
3. Spin up the production interface engine: `streamlit run Home.py`
